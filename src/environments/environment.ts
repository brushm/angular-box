// Default connection is to local Ganache. Ganache can be started in a separate terminal
// window with "npm run ganache"

export const environment = {
  production: false,
  infuraNetwork: "",
  infuraToken: "",
  ethUrl: "http://localhost:7545"
};
